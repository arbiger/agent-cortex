{
  "name": "agent-cortex",
  "version": "1.0.0",
  "description": "Standalone MCP server for shared memory across AI agents",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "build": "echo 'No build step needed - JavaScript'",
    "test": "node --test tests/*.test.js",
    "test:e2e:mvp2": "node tests/e2e/mvp2-hook-ingest.e2e.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "^12.10.0",
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
